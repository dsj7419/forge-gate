# T02 Implement the runtime actor

## Scope

Build the per-player runtime actor with a bounded queue.

## Notes

This legacy ticket carries no risk/change/blast metadata and no acceptance
criteria section, so the importer must flag those as ambiguous/missing.
