# Sandbox Epic

A deliberately boring, self-contained epic used to prove the Forge one-ticket orchestration loop on a
sterile, reversible target. It is not meant to build a useful feature — only to exercise:
select → branch/checkpoint → engineer → verify → semantic/scope verifiers → PM → commit gate.

Non-goals: real product work, dependency/config/package changes, anything outside `src/sandbox/**`.
